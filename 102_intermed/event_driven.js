
//  *** event listeners ***
// think like Java Swing library

// creating a listener on button
document.getElementById("lemons").onclick = () => console.log("click");

// can set breakpoint inside "Sources" of web console, which lets pausing for events, since a lot of event driven code


// Will be expanded later, but will be a while so this is just a stub